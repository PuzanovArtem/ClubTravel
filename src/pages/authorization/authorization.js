import { header } from '../../js/components/header.js';
import { validatorFunc } from '../../js/libs/validator.js';

document.addEventListener('DOMContentLoaded', () => {
    header();
    validatorFunc();
})